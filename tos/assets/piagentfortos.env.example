# ============================================================
# Pi Agent for TOS —— 可选配置（systemd EnvironmentFile）
# 改完执行: systemctl restart piagentfortos
#
# 注意：监听地址（HOSTNAME=127.0.0.1）与端口（PORT=18141）**固定在 systemd 单元里**，
# 不在此文件配置（安全敏感项写死；systemd 的 ExecStart 也不支持 shell 风格默认值）。
# 如需改端口：编辑 /etc/systemd/system/piagentfortos.service 后
#   systemctl daemon-reload && systemctl restart piagentfortos
# ============================================================

# ---- 时区（影响会话时间戳显示；默认跟随系统）----
# TZ=Asia/Shanghai

# ---- 关闭匿名遥测（默认已关）----
# NEXT_TELEMETRY_DISABLED=1

# ---- 模型 API 密钥（可选：也可以直接在应用界面里配置，界面配置会写入 agent 目录）----
# DEEPSEEK_API_KEY=sk-...
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...

# ---- 反向代理场景 ----
# 同源请求（Origin 与 Host 头一致）默认放行，一般无需配置。
# 仅当代理改写了 Host（导致 Origin ≠ Host）时，才需要显式列出允许的来源（逗号分隔）：
# PI_ALLOWED_ORIGINS=https://nas.example.com,https://pi.example.com:8443
# PUBLIC_BASE_URL=https://nas.example.com/piagentfortos/

# ---- 长期记忆（LTM）与调试 ----
# PI_LOG_LEVEL=info
